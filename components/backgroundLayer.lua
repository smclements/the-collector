BackgroundLayer = Class{
    init = function(self,indices,scale,color,scrollSpeed,x,y)
        self.indices = indices
        self.scale = scale
        self.color = color
        self.scrollSpeed = scrollSpeed
        self.x = x
        self.y = y
    end;
}
